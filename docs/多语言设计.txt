# 多语言开发约束

多语言必须从项目第一阶段开始设计，使用 i18n 实现，并按 Module 管理语言包。

每个 Module 必须提供：

```text
locales/zh-CN.json
locales/en.json
```

约束：

- 页面、菜单、表单、校验、错误提示和通知中的用户可见文本禁止硬编码。
- 新增或修改用户可见文本时，中文和英文语言包必须同时更新。
- Core 只提供 i18n 基础能力，不放业务 Module 的翻译内容。
- 权限名称、Resource 字段标题和 Navigation 标签也必须纳入语言包。
